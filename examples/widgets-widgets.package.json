{
  "name": "@sourcecred/widgets",
  "version": "0.0.1-alpha",
  "private": true,
  "dependencies": {
    "esm": "^3.2.25",
    "got": "^9.6.0",
    "keyv-fs-sync": "^0.1.0",
    "mkdirp": "^0.5.1",
    "sharp": "^0.23.0"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "prettier": "^1.18.2",
    "rimraf": "^3.0.0"
  },
  "scripts": {
    "prettify": "prettier --write '**/*.js'",
    "check-pretty": "prettier --list-different '**/*.js'",
    "test": "yarn run unit && yarn run --silent check-pretty",
    "unit": "jest"
  },
  "license": "(MIT OR Apache-2.0)"
}